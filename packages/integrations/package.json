{
  "name": "@lodestar/integrations",
  "version": "0.1.0",
  "private": true,
  "description": "External API clients through one egress gateway (allowlist, redirect re-check, rate limits, caching) + the install-time artifact downloader.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@lodestar/shared": "workspace:*"
  },
  "devDependencies": {
    "@lodestar/data": "workspace:*"
  }
}
