{
  "name": "@lodestar/intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Layer 1 — the deterministic intelligence engine (Assay thresholds/verdicts, scoring, optimization). All numbers, zero hallucination. Pure: depends on @lodestar/shared only.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@lodestar/shared": "workspace:*"
  }
}
